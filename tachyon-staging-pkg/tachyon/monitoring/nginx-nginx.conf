events {
    worker_connections 1024;
}

http {
    server {
        listen 8080;

        location /nginx_status {
            stub_status on;
            access_log off;
            allow all;
        }

        location / {
            return 404;
        }
    }
}
