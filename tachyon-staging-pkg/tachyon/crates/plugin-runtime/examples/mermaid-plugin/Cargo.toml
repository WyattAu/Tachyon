[package]
name = "mermaid-plugin"
version = "0.1.0"
edition = "2021"
description = "Tachyon plugin that renders Mermaid diagrams from code blocks"

[lib]
crate-type = ["cdylib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
