[package]
name = "csv-plugin"
version = "0.1.0"
edition = "2021"
description = "Tachyon plugin that renders CSV/TSV code blocks as HTML tables"

[lib]
crate-type = ["cdylib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
