[package]
name = "tachyon-editor"
version = "0.1.0"
license = { workspace = true }
edition.workspace = true
description = "Native Rust text editor engine for Tachyon"

[dependencies]
ropey = "1.6"
unicode-segmentation = "1.12"
regex = "1"
serde = { version = "1", features = ["derive"] }
thiserror = "2"
tracing = "0.1"
yrs = "0.25"

# tree-sitter highlighting (native-only; excluded on wasm32)
tree-sitter = { version = "0.25", optional = true }
tree-sitter-highlight = { version = "0.25", optional = true }
tree-sitter-rust = { version = "0.24", optional = true }
tree-sitter-python = { version = "0.25", optional = true }
tree-sitter-javascript = { version = "0.25", optional = true }
tree-sitter-typescript = { version = "0.23", optional = true }
tree-sitter-json = { version = "0.21", optional = true }
tree-sitter-yaml = { version = "0.7", optional = true }
tree-sitter-css = { version = "0.25", optional = true }
tree-sitter-bash = { version = "0.25", optional = true }
tree-sitter-html = { version = "0.23", optional = true }

[features]
staging = []
native-tree-sitter = [
  "dep:tree-sitter",
  "dep:tree-sitter-highlight",
  "dep:tree-sitter-rust",
  "dep:tree-sitter-python",
  "dep:tree-sitter-javascript",
  "dep:tree-sitter-typescript",
  "dep:tree-sitter-json",
  "dep:tree-sitter-yaml",
  "dep:tree-sitter-css",
  "dep:tree-sitter-bash",
  "dep:tree-sitter-html",
]
