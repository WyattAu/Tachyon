//! Factory functions for creating test data
//!
//! Provides factory methods to generate test entities with randomized or deterministic data.
//!
//! Note: These are stub factories pending implementation of the corresponding types.

/// Placeholder factory for test data
pub struct PlaceholderFactory;

impl PlaceholderFactory {
    /// Create a new placeholder
    pub fn new_placeholder() -> String {
        "placeholder".to_string()
    }
}
