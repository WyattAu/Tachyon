# Tachyon
JIT documentation server with internal developer portal, the build step latency inherently in traditional SSG can be eliminated by reactive file watching on the host file system, while still able to compile as SSG if needed.
